<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxl.ai.api.business.supplier.mapper.SupplierMapper">

	<resultMap id="Supplier" type="com.xxl.ai.api.business.supplier.model.entity.Supplier" >
		<result column="id" property="id" />
		<result column="space_id" property="spaceId" />
		<result column="name" property="name" />
		<result column="base_url" property="baseUrl" />
		<result column="api_key" property="apiKey" />
		<result column="status" property="status" />
		<result column="remark" property="remark" />
		<result column="add_time" property="addTime" />
		<result column="update_time" property="updateTime" />
	</resultMap>

	<sql id="Base_Column_List">
		t.id,
		t.space_id,
		t.name,
		t.base_url,
		t.api_key,
		t.status,
		t.remark,
		t.add_time,
		t.update_time
	</sql>

	<insert id="insert" parameterType="com.xxl.ai.api.business.supplier.model.entity.Supplier" useGeneratedKeys="true" keyProperty="id" >
		INSERT INTO xxl_ai_supplier (
			space_id, name, base_url, api_key, status, remark, add_time, update_time
		) VALUES (
			 #{spaceId}, #{name}, #{baseUrl}, #{apiKey}, #{status}, #{remark}, NOW(), NOW()
		 );
	</insert>

	<delete id="delete" parameterType="java.util.HashMap">
		DELETE FROM xxl_ai_supplier WHERE id = #{id}
	</delete>

	<delete id="deleteByIds" parameterType="java.util.HashMap">
		DELETE FROM xxl_ai_supplier
		WHERE id in
		<foreach collection="ids" item="item" open="(" close=")" separator="," >
			#{item}
		</foreach>
	</delete>

	<update id="update" parameterType="com.xxl.ai.api.business.supplier.model.entity.Supplier" >
		UPDATE xxl_ai_supplier
		SET name = #{name},
			base_url = #{baseUrl},
			api_key = #{apiKey},
			status = #{status},
			remark = #{remark},
			update_time = NOW()
		WHERE id = #{id}
	</update>

	<select id="load" parameterType="java.util.HashMap" resultMap="Supplier">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_supplier AS t
		WHERE t.id = #{id}
	</select>

	<select id="listBySpace" parameterType="java.util.HashMap" resultMap="Supplier">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_supplier AS t
		WHERE t.space_id = #{spaceId}
		ORDER BY t.id ASC
	</select>

	<select id="pageList" parameterType="java.util.HashMap" resultMap="Supplier">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_supplier AS t
		WHERE t.space_id = #{spaceId}
		<if test="name != null and name != ''">
			AND t.name like CONCAT(CONCAT('%', #{name}), '%')
		</if>
		<if test="status gt -1">
			AND t.status = #{status}
		</if>
		ORDER BY t.id ASC
		LIMIT #{offset}, #{pagesize}
	</select>

	<select id="pageListCount" parameterType="java.util.HashMap" resultType="int">
		SELECT count(1)
		FROM xxl_ai_supplier AS t
		WHERE t.space_id = #{spaceId}
		<if test="name != null and name != ''">
			AND t.name like CONCAT(CONCAT('%', #{name}), '%')
		</if>
		<if test="status gt -1">
			AND t.status = #{status}
		</if>
	</select>

</mapper>