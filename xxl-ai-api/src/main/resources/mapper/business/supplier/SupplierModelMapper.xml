<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxl.ai.api.business.supplier.mapper.SupplierModelMapper">

	<resultMap id="SupplierModel" type="com.xxl.ai.api.business.supplier.model.entity.SupplierModel" >
		<result column="id" property="id" />
		<result column="supplier_id" property="supplierId" />
		<result column="name" property="name" />
		<result column="model" property="model" />
		<result column="type" property="type" />
		<result column="status" property="status" />
		<result column="add_time" property="addTime" />
		<result column="update_time" property="updateTime" />
	</resultMap>

	<sql id="Base_Column_List">
		t.id,
		t.supplier_id,
		t.name,
		t.model,
		t.type,
		t.status,
		t.add_time,
		t.update_time
	</sql>

	<insert id="insert" parameterType="com.xxl.ai.api.business.supplier.model.entity.SupplierModel" useGeneratedKeys="true" keyProperty="id" >
		INSERT INTO xxl_ai_supplier_model (
			supplier_id, name, model, type, status, add_time, update_time
		) VALUES (
			 #{supplierId}, #{name}, #{model}, #{type}, #{status}, NOW(), NOW()
		 );
	</insert>

	<delete id="delete" parameterType="java.util.HashMap">
		DELETE FROM xxl_ai_supplier_model WHERE id = #{id}
	</delete>

	<delete id="deleteByIds" parameterType="java.util.HashMap">
		DELETE FROM xxl_ai_supplier_model
		WHERE id in
		<foreach collection="ids" item="item" open="(" close=")" separator="," >
			#{item}
		</foreach>
	</delete>

	<update id="update" parameterType="com.xxl.ai.api.business.supplier.model.entity.SupplierModel" >
		UPDATE xxl_ai_supplier_model
		SET name = #{name},
			model = #{model},
			type = #{type},
			status = #{status},
			update_time = NOW()
		WHERE id = #{id}
	</update>

	<select id="load" parameterType="java.util.HashMap" resultMap="SupplierModel">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_supplier_model AS t
		WHERE t.id = #{id}
	</select>

	<select id="listBySupplier" parameterType="java.util.HashMap" resultMap="SupplierModel">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_supplier_model AS t
		WHERE t.supplier_id = #{supplierId}
		ORDER BY t.id ASC
	</select>

	<select id="pageList" parameterType="java.util.HashMap" resultMap="SupplierModel">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_supplier_model AS t
		WHERE t.supplier_id = #{supplierId}
		<if test="name != null and name != ''">
			AND t.name like CONCAT(CONCAT('%', #{name}), '%')
		</if>
		<if test="type gt -1">
			AND t.type = #{type}
		</if>
		ORDER BY t.id ASC
		LIMIT #{offset}, #{pagesize}
	</select>

	<select id="pageListCount" parameterType="java.util.HashMap" resultType="int">
		SELECT count(1)
		FROM xxl_ai_supplier_model AS t
		WHERE t.supplier_id = #{supplierId}
		<if test="name != null and name != ''">
			AND t.name like CONCAT(CONCAT('%', #{name}), '%')
		</if>
		<if test="type gt -1">
			AND t.type = #{type}
		</if>
	</select>

</mapper>