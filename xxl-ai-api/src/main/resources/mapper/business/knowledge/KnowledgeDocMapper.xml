<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxl.ai.api.business.knowledge.doc.mapper.KnowledgeDocMapper">

	<resultMap id="KnowledgeDoc" type="com.xxl.ai.api.business.knowledge.doc.model.entity.KnowledgeDoc" >
		<result column="id" property="id" />
		<result column="space_id" property="spaceId" />
		<result column="base_id" property="baseId" />
		<result column="name" property="name" />
		<result column="content" property="content" />
		<result column="chunk_count" property="chunkCount" />
		<result column="status" property="status" />
		<result column="add_time" property="addTime" />
		<result column="update_time" property="updateTime" />
	</resultMap>

	<sql id="Base_Column_List">
		t.id,
		t.space_id,
		t.base_id,
		t.name,
		t.content,
		t.chunk_count,
		t.status,
		t.add_time,
		t.update_time
	</sql>

	<insert id="insert" parameterType="com.xxl.ai.api.business.knowledge.doc.model.entity.KnowledgeDoc" useGeneratedKeys="true" keyProperty="id" >
		INSERT INTO xxl_ai_knowledge_doc (
			space_id, base_id, name, content, chunk_count, status, add_time, update_time
		) VALUES (
			 #{spaceId}, #{baseId}, #{name}, #{content}, #{chunkCount}, #{status}, NOW(), NOW()
		 );
	</insert>

	<delete id="delete" parameterType="java.util.HashMap">
		DELETE FROM xxl_ai_knowledge_doc WHERE id = #{id}
	</delete>

	<delete id="deleteByIds" parameterType="java.util.HashMap">
		DELETE FROM xxl_ai_knowledge_doc
		WHERE id in
		<foreach collection="ids" item="item" open="(" close=")" separator="," >
			#{item}
		</foreach>
	</delete>

	<delete id="deleteByBaseId" parameterType="java.util.HashMap">
		DELETE FROM xxl_ai_knowledge_doc WHERE base_id = #{baseId}
	</delete>

	<update id="update" parameterType="com.xxl.ai.api.business.knowledge.doc.model.entity.KnowledgeDoc" >
		UPDATE xxl_ai_knowledge_doc
		SET name = #{name},
			content = #{content},
			chunk_count = #{chunkCount},
			status = #{status},
			update_time = NOW()
		WHERE id = #{id}
	</update>

	<select id="load" parameterType="java.util.HashMap" resultMap="KnowledgeDoc">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_knowledge_doc AS t
		WHERE t.id = #{id}
	</select>

	<select id="listByBase" parameterType="java.util.HashMap" resultMap="KnowledgeDoc">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_knowledge_doc AS t
		WHERE t.base_id = #{baseId}
		ORDER BY t.id ASC
	</select>

	<select id="listByBaseIds" parameterType="java.util.HashMap" resultMap="KnowledgeDoc">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_knowledge_doc AS t
		WHERE t.base_id in
		<foreach collection="ids" item="item" open="(" close=")" separator="," >
			#{item}
		</foreach>
	</select>

	<select id="pageList" parameterType="java.util.HashMap" resultMap="KnowledgeDoc">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_knowledge_doc AS t
		WHERE t.base_id = #{baseId}
		<if test="name != null and name != ''">
			AND t.name like CONCAT(CONCAT('%', #{name}), '%')
		</if>
		<if test="status gt -1">
			AND t.status = #{status}
		</if>
		ORDER BY t.id ASC
		LIMIT #{offset}, #{pagesize}
	</select>

	<select id="pageListCount" parameterType="java.util.HashMap" resultType="int">
		SELECT count(1)
		FROM xxl_ai_knowledge_doc AS t
		WHERE t.base_id = #{baseId}
		<if test="name != null and name != ''">
			AND t.name like CONCAT(CONCAT('%', #{name}), '%')
		</if>
		<if test="status gt -1">
			AND t.status = #{status}
		</if>
	</select>

</mapper>