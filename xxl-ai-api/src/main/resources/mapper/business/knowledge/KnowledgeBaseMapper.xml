<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxl.ai.api.business.knowledge.base.mapper.KnowledgeBaseMapper">

	<resultMap id="KnowledgeBase" type="com.xxl.ai.api.business.knowledge.base.model.entity.KnowledgeBase" >
		<result column="id" property="id" />
		<result column="space_id" property="spaceId" />
		<result column="name" property="name" />
		<result column="description" property="description" />
		<result column="embed_supplier_id" property="embedSupplierId" />
		<result column="embed_model_id" property="embedModelId" />
		<result column="chunk_size" property="chunkSize" />
		<result column="chunk_overlap" property="chunkOverlap" />
		<result column="top_k" property="topK" />
		<result column="status" property="status" />
		<result column="add_time" property="addTime" />
		<result column="update_time" property="updateTime" />
	</resultMap>

	<sql id="Base_Column_List">
		t.id,
		t.space_id,
		t.name,
		t.description,
		t.embed_supplier_id,
		t.embed_model_id,
		t.chunk_size,
		t.chunk_overlap,
		t.top_k,
		t.status,
		t.add_time,
		t.update_time
	</sql>

	<insert id="insert" parameterType="com.xxl.ai.api.business.knowledge.base.model.entity.KnowledgeBase" useGeneratedKeys="true" keyProperty="id" >
		INSERT INTO xxl_ai_knowledge_base (
			space_id, name, description, embed_supplier_id, embed_model_id,
			chunk_size, chunk_overlap, top_k, status, add_time, update_time
		) VALUES (
			 #{spaceId}, #{name}, #{description}, #{embedSupplierId}, #{embedModelId},
			 #{chunkSize}, #{chunkOverlap}, #{topK}, #{status}, NOW(), NOW()
		 );
	</insert>

	<delete id="delete" parameterType="java.util.HashMap">
		DELETE FROM xxl_ai_knowledge_base WHERE id = #{id}
	</delete>

	<delete id="deleteByIds" parameterType="java.util.HashMap">
		DELETE FROM xxl_ai_knowledge_base
		WHERE id in
		<foreach collection="ids" item="item" open="(" close=")" separator="," >
			#{item}
		</foreach>
	</delete>

	<update id="update" parameterType="com.xxl.ai.api.business.knowledge.base.model.entity.KnowledgeBase" >
		UPDATE xxl_ai_knowledge_base
		SET name = #{name},
			description = #{description},
			embed_supplier_id = #{embedSupplierId},
			embed_model_id = #{embedModelId},
			chunk_size = #{chunkSize},
			chunk_overlap = #{chunkOverlap},
			top_k = #{topK},
			status = #{status},
			update_time = NOW()
		WHERE id = #{id}
	</update>

	<select id="load" parameterType="java.util.HashMap" resultMap="KnowledgeBase">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_knowledge_base AS t
		WHERE t.id = #{id}
	</select>

	<select id="listBySpace" parameterType="java.util.HashMap" resultMap="KnowledgeBase">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_knowledge_base AS t
		WHERE t.space_id = #{spaceId}
		ORDER BY t.id ASC
	</select>

	<select id="countBySpaceId" parameterType="java.util.HashMap" resultType="int">
		SELECT count(1)
		FROM xxl_ai_knowledge_base AS t
		WHERE t.space_id = #{spaceId}
	</select>

	<select id="listByIds" parameterType="java.util.HashMap" resultMap="KnowledgeBase">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_knowledge_base AS t
		WHERE t.id in
		<foreach collection="ids" item="item" open="(" close=")" separator="," >
			#{item}
		</foreach>
	</select>

	<select id="pageList" parameterType="java.util.HashMap" resultMap="KnowledgeBase">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_knowledge_base AS t
		WHERE t.space_id = #{spaceId}
		<if test="name != null and name != ''">
			AND t.name like CONCAT(CONCAT('%', #{name}), '%')
		</if>
		<if test="status gt -1">
			AND t.status = #{status}
		</if>
		ORDER BY t.id ASC
		LIMIT #{offset}, #{pagesize}
	</select>

	<select id="pageListCount" parameterType="java.util.HashMap" resultType="int">
		SELECT count(1)
		FROM xxl_ai_knowledge_base AS t
		WHERE t.space_id = #{spaceId}
		<if test="name != null and name != ''">
			AND t.name like CONCAT(CONCAT('%', #{name}), '%')
		</if>
		<if test="status gt -1">
			AND t.status = #{status}
		</if>
	</select>

</mapper>