<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxl.ai.api.business.mcp.mapper.McpMapper">

	<resultMap id="Mcp" type="com.xxl.ai.api.business.mcp.model.entity.Mcp" >
		<result column="id" property="id" />
		<result column="space_id" property="spaceId" />
		<result column="name" property="name" />
		<result column="type" property="type" />
		<result column="url" property="url" />
		<result column="headers" property="headers" />
		<result column="config" property="config" />
		<result column="description" property="description" />
		<result column="status" property="status" />
		<result column="add_time" property="addTime" />
		<result column="update_time" property="updateTime" />
	</resultMap>

	<sql id="Base_Column_List">
		t.id,
		t.space_id,
		t.name,
		t.type,
		t.url,
		t.headers,
		t.config,
		t.description,
		t.status,
		t.add_time,
		t.update_time
	</sql>

	<insert id="insert" parameterType="com.xxl.ai.api.business.mcp.model.entity.Mcp" useGeneratedKeys="true" keyProperty="id" >
		INSERT INTO xxl_ai_mcp (
			space_id, name, type, url, headers, config, description, status, add_time, update_time
		) VALUES (
			 #{spaceId}, #{name}, #{type}, #{url}, #{headers}, #{config}, #{description}, #{status}, NOW(), NOW()
		 );
	</insert>

	<delete id="delete" parameterType="java.util.HashMap">
		DELETE FROM xxl_ai_mcp WHERE id = #{id}
	</delete>

	<delete id="deleteByIds" parameterType="java.util.HashMap">
		DELETE FROM xxl_ai_mcp
		WHERE id in
		<foreach collection="ids" item="item" open="(" close=")" separator="," >
			#{item}
		</foreach>
	</delete>

	<update id="update" parameterType="com.xxl.ai.api.business.mcp.model.entity.Mcp" >
		UPDATE xxl_ai_mcp
		SET name = #{name},
			type = #{type},
			url = #{url},
			headers = #{headers},
			config = #{config},
			description = #{description},
			status = #{status},
			update_time = NOW()
		WHERE id = #{id}
	</update>

	<select id="load" parameterType="java.util.HashMap" resultMap="Mcp">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_mcp AS t
		WHERE t.id = #{id}
	</select>

	<select id="listBySpace" parameterType="java.util.HashMap" resultMap="Mcp">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_mcp AS t
		WHERE t.space_id = #{spaceId}
		ORDER BY t.id ASC
	</select>

	<select id="countBySpaceId" parameterType="java.util.HashMap" resultType="int">
		SELECT count(1)
		FROM xxl_ai_mcp AS t
		WHERE t.space_id = #{spaceId}
	</select>

	<select id="listByIds" parameterType="java.util.HashMap" resultMap="Mcp">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_mcp AS t
		WHERE t.id in
		<foreach collection="ids" item="item" open="(" close=")" separator="," >
			#{item}
		</foreach>
	</select>

	<select id="pageList" parameterType="java.util.HashMap" resultMap="Mcp">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_mcp AS t
		WHERE t.space_id = #{spaceId}
		<if test="name != null and name != ''">
			AND t.name like CONCAT(CONCAT('%', #{name}), '%')
		</if>
		<if test="status gt -1">
			AND t.status = #{status}
		</if>
		ORDER BY t.id ASC
		LIMIT #{offset}, #{pagesize}
	</select>

	<select id="pageListCount" parameterType="java.util.HashMap" resultType="int">
		SELECT count(1)
		FROM xxl_ai_mcp AS t
		WHERE t.space_id = #{spaceId}
		<if test="name != null and name != ''">
			AND t.name like CONCAT(CONCAT('%', #{name}), '%')
		</if>
		<if test="status gt -1">
			AND t.status = #{status}
		</if>
	</select>

</mapper>