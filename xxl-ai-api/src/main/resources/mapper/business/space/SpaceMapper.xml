<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxl.ai.api.business.space.mapper.SpaceMapper">

	<resultMap id="Space" type="com.xxl.ai.api.business.space.model.entity.Space" >
		<result column="id" property="id" />
		<result column="name" property="name" />
		<result column="code" property="code" />
		<result column="status" property="status" />
		<result column="remark" property="remark" />
		<result column="add_time" property="addTime" />
		<result column="update_time" property="updateTime" />
	</resultMap>

	<sql id="Base_Column_List">
		t.id,
		t.name,
		t.code,
		t.status,
		t.remark,
		t.add_time,
		t.update_time
	</sql>

	<insert id="insert" parameterType="com.xxl.ai.api.business.space.model.entity.Space" useGeneratedKeys="true" keyProperty="id" >
		INSERT INTO xxl_ai_space (
			name, code, status, remark, add_time, update_time
		) VALUES (
			 #{name}, #{code}, #{status}, #{remark}, NOW(), NOW()
		 );
	</insert>

	<delete id="delete" parameterType="java.util.HashMap">
		DELETE FROM xxl_ai_space WHERE id = #{id}
	</delete>

	<delete id="deleteByIds" parameterType="java.util.HashMap">
		DELETE FROM xxl_ai_space
		WHERE id in
		<foreach collection="ids" item="item" open="(" close=")" separator="," >
			#{item}
		</foreach>
	</delete>

	<update id="update" parameterType="com.xxl.ai.api.business.space.model.entity.Space" >
		UPDATE xxl_ai_space
		SET name = #{name},
			code = #{code},
			status = #{status},
			remark = #{remark},
			update_time = NOW()
		WHERE id = #{id}
	</update>

	<select id="load" parameterType="java.util.HashMap" resultMap="Space">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_space AS t
		WHERE t.id = #{id}
	</select>

	<select id="loadByCode" parameterType="java.util.HashMap" resultMap="Space">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_space AS t
		WHERE t.code = #{code}
	</select>

	<select id="listAll" resultMap="Space">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_space AS t
		ORDER BY t.id ASC
	</select>

	<select id="listByIds" parameterType="java.util.HashMap" resultMap="Space">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_space AS t
		WHERE t.id in
		<foreach collection="ids" item="item" open="(" close=")" separator="," >
			#{item}
		</foreach>
	</select>

	<select id="listByUserId" parameterType="java.util.HashMap" resultMap="Space">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_space AS t
		WHERE t.id in (
			SELECT us.space_id
			FROM xxl_ai_user_space AS us
			WHERE us.user_id = #{userId}
		)
		ORDER BY t.id ASC
	</select>

	<select id="pageList" parameterType="java.util.HashMap" resultMap="Space">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_space AS t
		<trim prefix="WHERE" prefixOverrides="AND | OR" >
			<if test="name != null and name != ''">
				AND t.name like CONCAT(CONCAT('%', #{name}), '%')
			</if>
			<if test="status gt -1">
				AND t.status = #{status}
			</if>
		</trim>
		ORDER BY t.id ASC
		LIMIT #{offset}, #{pagesize}
	</select>

	<select id="pageListCount" parameterType="java.util.HashMap" resultType="int">
		SELECT count(1)
		FROM xxl_ai_space AS t
		<trim prefix="WHERE" prefixOverrides="AND | OR" >
			<if test="name != null and name != ''">
				AND t.name like CONCAT(CONCAT('%', #{name}), '%')
			</if>
			<if test="status gt -1">
				AND t.status = #{status}
			</if>
		</trim>
	</select>

</mapper>