<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxl.ai.api.framework.mapper.system.UserMapper">

	<resultMap id="User" type="com.xxl.ai.api.framework.model.entity.User" >
		<result column="id" property="id" />
		<result column="username" property="username" />
	    <result column="password" property="password" />
	    <result column="token" property="token" />
	    <result column="status" property="status" />
		<result column="role" property="role" />
		<result column="real_name" property="realName" />
		<result column="email" property="email" />
		<result column="add_time" property="addTime" />
		<result column="update_time" property="updateTime" />
	</resultMap>

	<sql id="Base_Column_List">
		t.id,
		t.username,
		t.password,
		t.token,
		t.status,
		t.role,
		t.real_name,
		t.email,
		t.add_time,
		t.update_time
	</sql>

	<insert id="insert" parameterType="com.xxl.ai.api.framework.model.entity.User" useGeneratedKeys="true" keyProperty="id" >
		INSERT INTO xxl_ai_user (
			username,
			password,
			token,
			status,
			role,
			real_name,
			email,
			add_time,
			update_time
		) VALUES (
			 #{username},
			 #{password},
			 #{token},
			 #{status},
			 #{role},
			 #{realName},
			 #{email},
			 NOW(),
			 NOW()
		 );
	</insert>

	<delete id="delete" parameterType="java.util.HashMap">
		DELETE
		FROM xxl_ai_user
		WHERE id = #{id}
	</delete>

	<delete id="deleteByIds" parameterType="java.util.HashMap">
		DELETE
		FROM xxl_ai_user
		WHERE id in
		<foreach collection="ids" item="item" open="(" close=")" separator="," >
			#{item}
		</foreach>
	</delete>

	<update id="update" parameterType="com.xxl.ai.api.framework.model.entity.User" >
		UPDATE xxl_ai_user
		SET
			<if test="password != null and password != ''">
				password = #{password},
			</if>
			token = #{token},
			status = #{status},
			role = #{role},
			real_name = #{realName},
			email = #{email},
			update_time = NOW()
		WHERE id = #{id}
	</update>

	<select id="loadByUserName" parameterType="java.util.HashMap" resultMap="User">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_user AS t
		WHERE t.username = #{username}
	</select>

	<select id="load" parameterType="java.util.HashMap" resultMap="User">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_user AS t
		WHERE t.id = #{id}
	</select>

	<select id="loadByIds" parameterType="java.util.HashMap" resultMap="User">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_user AS t
		WHERE t.id in
		<foreach collection="ids" item="item" open="(" close=")" separator="," >
			#{item}
		</foreach>
	</select>

	<select id="pageList" parameterType="java.util.HashMap" resultMap="User">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_ai_user AS t
		<trim prefix="WHERE" prefixOverrides="AND | OR" >
			<if test="username != null and username != ''">
				AND t.username like CONCAT(CONCAT('%', #{username}), '%')
			</if>
			<if test="status gt -1">
				AND t.status = #{status}
			</if>
		</trim>
		ORDER BY username ASC
		LIMIT #{offset}, #{pagesize}
	</select>

	<select id="pageListCount" parameterType="java.util.HashMap" resultType="int">
		SELECT count(1)
		FROM xxl_ai_user AS t
		<trim prefix="WHERE" prefixOverrides="AND | OR" >
			<if test="username != null and username != ''">
				AND t.username like CONCAT(CONCAT('%', #{username}), '%')
			</if>
			<if test="status gt -1">
				AND t.status = #{status}
			</if>
		</trim>
	</select>

	<update id="updateToken" parameterType="java.util.HashMap"  >
		UPDATE xxl_ai_user
		SET token = #{token},
			update_time = NOW()
		WHERE id = #{id}
	</update>

</mapper>